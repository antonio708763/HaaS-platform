# Immich Database (PostgreSQL + pgvector)
DB_HOSTNAME=database
DB_USERNAME=postgres
DB_PASSWORD=change_me_secure_password
DB_DATABASE=immich

# Redis
REDIS_HOSTNAME=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# Immich Configuration
IMMICH_VERSION=release

# Logging
IMMICH_LOG_LEVEL=info

# Upload location (host path: ./library)
UPLOAD_LOCATION=/usr/src/app/upload

# Machine Learning
MACHINE_LEARNING_ENABLED=true

# Optional: External access (reverse proxy)
IMMICH_WEB_URL=http://photos.lab.local

# GPU Acceleration (AMD radeon/r600)
LIBVA_DRIVER_NAME=r600

# Security (adjust for production)
JWT_SECRET=change_me_32_random_characters
